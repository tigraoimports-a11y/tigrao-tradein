# Status — 2026-04-06

Resumo do dia na branch `fix/edit-item-acaminho`.

## Commits de hoje (na branch, ainda não em main)

| Hash | Escopo | Descrição |
|------|--------|-----------|
| `d80d089` | vendas | Persistir campos completos da troca por categoria |
| `0609a4b` | vendas | Incluir 2º cartão em recalcVendido / preview / conferência |
| `aa5c4d9` `3b70536` | vendas | Somar 2º cartão no preço vendido (single product) |
| `a174149` | vendas | Evitar loop infinito no recálculo do 2º cartão |
| `807e29a` | vendas | Mover useEffect do 2º cartão antes do early return (fix React #310) |
| `875faeb` | grade | `[GRADE_APLUS]` → `[GRADE_A+]` em todos os arquivos |
| `46290e9` | vendas | Proteção anti-duplicidade de serial no POST `/api/vendas` |
| `c09c689` | perf / search | Estabiliza `useAutoRefetch` + specs no DetailModal do SuperSearch |
| `f1d3f23` | search | Specs no DetailModal respeitam categoria do produto |
| `83bf446` | search | Origem apenas para iPhone |
| `1d63bc8` | estoque | Card title overrides agora persistem (localStorage + merge + logs) |
| `c599120` | estoque | Separa Apple Watch GPS vs GPS+CEL + normaliza resumo de cores |

## Migrations SQL (status)

| Arquivo | Status |
|---------|--------|
| `supabase/migrations/20260406_vendas_troca_serial_imei.sql` | ✅ Aplicada |
| `supabase/migrations/20260406_rename_grade_aplus.sql` | ✅ Aplicada |
| `supabase/migrations/20260406_fix_duplicate_serial.sql` | ✅ Aplicada (corrigiu G0NF5T3L0D52) |
| `supabase/migrations/20260406_app_settings_grants.sql` | ✅ Aplicada |

Todas as migrations necessárias estão no banco. Nada pendente.

## Pendente para virar produção

1. **Merge do PR** `fix/edit-item-acaminho` → `main` no GitHub (branch protection bloqueia push direto).
2. Aguardar deploy automático do Vercel.
3. Verificar edge cache do Vercel após deploy (reference: `feedback_vercel_cache`).

## Ganhos de performance de hoje

- `lib/useAutoRefetch.ts` reescrito com ref: antes, cada render recriava o `setInterval` e o listener, causando polling falho e lag em ~15 páginas admin. Agora o efeito monta uma só vez por página.
- `focus` listener → `visibilitychange`: só refaz fetch ao voltar de aba oculta, não a cada clique fora da janela.
- Polling de `card_title_overrides` na página de estoque: 15s → 60s, + `visibilitychange`.
- Fetch inicial de overrides hidrata do localStorage (UI não fica "piscando" esperando o banco).

## Pontos que ficaram tabelados (para outro dia, sem urgência)

- Modal `Detalhes do Item` dentro de `app/admin/estoque/page.tsx` (~linha 4534) também poderia parsear tags por categoria, espelhando o que já foi feito no `SuperSearch DetailModal`.
- Bug antigo do balanço na exibição — confirmar se a normalização de cores do header do card resolve 100% após o deploy.

## Regras de ouro para amanhã

- Não adicionar polling novo sem `visibilitychange` guard.
- Todo `useAutoRefetch` continua funcionando com função inline, mas se precisar de outro polling, seguir o padrão do ref.
- Nada de `.catch(() => {})` silencioso em saves críticos — logar no console pelo menos.
- Migrations de SQL: manter em `supabase/migrations/` com o prefixo de data.
- Para Apple Watch, sempre tratar GPS e GPS+CEL como modelos distintos (inclusive em relatórios/balanço).
